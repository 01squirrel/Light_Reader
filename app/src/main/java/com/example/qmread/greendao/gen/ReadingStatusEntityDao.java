package com.example.qmread.greendao.gen;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.SqlUtils;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.example.qmread.bean.BookEntity;
import com.example.qmread.bean.ChapterEntity;
import com.example.qmread.bean.OrdinaryUserEntity;
import com.example.qmread.bean.RegisteredUserEntity;

import com.example.qmread.bean.ReadingStatusEntity;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "READING_STATUS_ENTITY".
*/
public class ReadingStatusEntityDao extends AbstractDao<ReadingStatusEntity, Long> {

    public static final String TABLENAME = "READING_STATUS_ENTITY";

    /**
     * Properties of entity ReadingStatusEntity.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property StatusId = new Property(0, long.class, "statusId", true, "_id");
        public final static Property BookId = new Property(1, long.class, "bookId", false, "BOOK_ID");
        public final static Property ChapterId = new Property(2, long.class, "chapterId", false, "CHAPTER_ID");
        public final static Property ReadingDuration = new Property(3, Integer.class, "readingDuration", false, "READING_DURATION");
        public final static Property ReadingLocation = new Property(4, Integer.class, "readingLocation", false, "READING_LOCATION");
        public final static Property UserId = new Property(5, long.class, "userId", false, "USER_ID");
    }

    private DaoSession daoSession;


    public ReadingStatusEntityDao(DaoConfig config) {
        super(config);
    }
    
    public ReadingStatusEntityDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"READING_STATUS_ENTITY\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL ," + // 0: statusId
                "\"BOOK_ID\" INTEGER NOT NULL ," + // 1: bookId
                "\"CHAPTER_ID\" INTEGER NOT NULL ," + // 2: chapterId
                "\"READING_DURATION\" INTEGER," + // 3: readingDuration
                "\"READING_LOCATION\" INTEGER," + // 4: readingLocation
                "\"USER_ID\" INTEGER NOT NULL );"); // 5: userId
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_READING_STATUS_ENTITY__id ON \"READING_STATUS_ENTITY\"" +
                " (\"_id\" ASC);");
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"READING_STATUS_ENTITY\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ReadingStatusEntity entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getStatusId());
        stmt.bindLong(2, entity.getBookId());
        stmt.bindLong(3, entity.getChapterId());
 
        Integer readingDuration = entity.getReadingDuration();
        if (readingDuration != null) {
            stmt.bindLong(4, readingDuration);
        }
 
        Integer readingLocation = entity.getReadingLocation();
        if (readingLocation != null) {
            stmt.bindLong(5, readingLocation);
        }
        stmt.bindLong(6, entity.getUserId());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ReadingStatusEntity entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getStatusId());
        stmt.bindLong(2, entity.getBookId());
        stmt.bindLong(3, entity.getChapterId());
 
        Integer readingDuration = entity.getReadingDuration();
        if (readingDuration != null) {
            stmt.bindLong(4, readingDuration);
        }
 
        Integer readingLocation = entity.getReadingLocation();
        if (readingLocation != null) {
            stmt.bindLong(5, readingLocation);
        }
        stmt.bindLong(6, entity.getUserId());
    }

    @Override
    protected final void attachEntity(ReadingStatusEntity entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    @Override
    public ReadingStatusEntity readEntity(Cursor cursor, int offset) {
        ReadingStatusEntity entity = new ReadingStatusEntity( //
            cursor.getLong(offset + 0), // statusId
            cursor.getLong(offset + 1), // bookId
            cursor.getLong(offset + 2), // chapterId
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3), // readingDuration
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // readingLocation
            cursor.getLong(offset + 5) // userId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ReadingStatusEntity entity, int offset) {
        entity.setStatusId(cursor.getLong(offset + 0));
        entity.setBookId(cursor.getLong(offset + 1));
        entity.setChapterId(cursor.getLong(offset + 2));
        entity.setReadingDuration(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
        entity.setReadingLocation(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setUserId(cursor.getLong(offset + 5));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(ReadingStatusEntity entity, long rowId) {
        entity.setStatusId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(ReadingStatusEntity entity) {
        if(entity != null) {
            return entity.getStatusId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(ReadingStatusEntity entity) {
        throw new UnsupportedOperationException("Unsupported for entities with a non-null key");
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getBookEntityDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getChapterEntityDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T2", daoSession.getRegisteredUserEntityDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T3", daoSession.getOrdinaryUserEntityDao().getAllColumns());
            builder.append(" FROM READING_STATUS_ENTITY T");
            builder.append(" LEFT JOIN BOOK_ENTITY T0 ON T.\"BOOK_ID\"=T0.\"_id\"");
            builder.append(" LEFT JOIN CHAPTER_ENTITY T1 ON T.\"CHAPTER_ID\"=T1.\"_id\"");
            builder.append(" LEFT JOIN REGISTERED_USER_ENTITY T2 ON T.\"USER_ID\"=T2.\"_id\"");
            builder.append(" LEFT JOIN ORDINARY_USER_ENTITY T3 ON T.\"USER_ID\"=T3.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected ReadingStatusEntity loadCurrentDeep(Cursor cursor, boolean lock) {
        ReadingStatusEntity entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        BookEntity bookEntity = loadCurrentOther(daoSession.getBookEntityDao(), cursor, offset);
         if(bookEntity != null) {
            entity.setBookEntity(bookEntity);
        }
        offset += daoSession.getBookEntityDao().getAllColumns().length;

        ChapterEntity chapterEntity = loadCurrentOther(daoSession.getChapterEntityDao(), cursor, offset);
         if(chapterEntity != null) {
            entity.setChapterEntity(chapterEntity);
        }
        offset += daoSession.getChapterEntityDao().getAllColumns().length;

        RegisteredUserEntity registeredUserEntity = loadCurrentOther(daoSession.getRegisteredUserEntityDao(), cursor, offset);
         if(registeredUserEntity != null) {
            entity.setRegisteredUserEntity(registeredUserEntity);
        }
        offset += daoSession.getRegisteredUserEntityDao().getAllColumns().length;

        OrdinaryUserEntity ordinaryUserEntity = loadCurrentOther(daoSession.getOrdinaryUserEntityDao(), cursor, offset);
         if(ordinaryUserEntity != null) {
            entity.setOrdinaryUserEntity(ordinaryUserEntity);
        }

        return entity;    
    }

    public ReadingStatusEntity loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<ReadingStatusEntity> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<ReadingStatusEntity> list = new ArrayList<ReadingStatusEntity>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<ReadingStatusEntity> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<ReadingStatusEntity> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
